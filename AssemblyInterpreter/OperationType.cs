using System;
using System.Collections.Generic;
using System.Text;

namespace AssemblyInterpreter
{
    public enum OperationType
    {
        ADD,INC, SUB, MUL, DIV, DEC, MOV, XCHG, DB, DW, DD
    }
}
